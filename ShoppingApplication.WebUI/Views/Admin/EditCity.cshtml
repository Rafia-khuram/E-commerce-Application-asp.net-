@using ShoppingApplication.BOL
@model City
@{
    ViewBag.Title = "EditCity";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<div class="container">
    <div class="col-lg-4 col-lg-push-4 col-md-4 col-md-push-4">
        <div class="panel panel-default">
            <h2 class="text-center">Edit City</h2>
            <div class="panel-body">
                @using (Html.BeginForm("EditCity", "Admin", FormMethod.Post))
                {
                    <div class="form-group">
                        <div class="input-group form-inline">
                            <span class="input-group-addon">
                                <span class="fa fa-city"></span>
                            </span>
                            @Html.TextBoxFor(m => m.Name, new { @class = "form-control", placeholder = "City Name", required = "required" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="input-group form-inline">
                            <span class="input-group-addon">
                                <span class="fa fa-user-tag"></span>
                            </span>
                            @{
                                List<Country> Countries = ViewBag.Countries as List<Country>;
                            }

                            <input type="hidden" id="SelectedCountry" value="@Model.CountryId" />
                            <select name="CountryId" class="form-control">
                                @foreach (var item in Countries)
                                {
                                    <option value="@item.Id">@item.Name</option>
                                }
                            </select>
                        </div>
                    </div>
                    <div class="text-center">
                        <button class="btn btn-black" type="submit">Edit City</button>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
@section Scripts{
    <script>
            $("#CountryId").val($("#SelectedCountry").val());
    </script>

}
